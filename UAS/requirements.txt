# Pinned to the exact versions used to TRAIN the model.
# The .pkl is created by scikit-learn/lightgbm; loading it under a different
# major version can silently break or raise. Keep these pins for deployment.
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.4.4
xgboost==3.2.0
lightgbm==4.6.0
joblib==1.5.3
mlflow==3.13.0
streamlit==1.58.0
